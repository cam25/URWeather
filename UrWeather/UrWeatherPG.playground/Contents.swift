//: Playground - noun: a place where people can play

import UIKit
import Foundation
/*
let fizz: String = "fizz";
let buzz: String = "buzz";
let fizzBuzz: String = "fizzBuzz";
let height = 12;
let width = 10;
var firstInt: Int;
var secondInt: Int;


let a = 3.0, b = 4.0;

func greetings(name:String, lunchToday:String) ->String
{

    return "Hello \(name) today's lunch special is \(lunchToday)";
    
}

greetings("Cameron", lunchToday: "Pizza");

func calculateAverage(numberCollection: Int...) -> Int
{
    var sum = 0;
    var findAverage = 0;
    for number in numberCollection
    {
        sum += number;
        
        findAverage++;
     
        
        
    }
    
    return sum / findAverage;
}
calculateAverage(32,53,66)


func square(number: Double) ->Double{
    return number * number;
    
}
let c = sqrt(square(a) + square(b));
print(c);

func calculateArea(height: Int, width: Int) -> Int {

    
    return height * width;
    
}
func sortArray(num1: Int, num2:Int) ->Bool
{
    
    return num1 < num2;
    
}


var arrayA:NSArray = ["Mike","Tom","Dick","Harry"];

var index = Int();

var numbers = [20,20,21,33,55,55, 19, 7,7, 12];

for n in numbers
{
    if n % 2 == 0
    {
        print(n.description)
    }
}

var mySet = Set<Int>(numbers)

numbers = Array(mySet)

numbers.sortInPlace {$0 < $1}

print(numbers)


var prevItem: Int = 0;
for index = 0; index < numbers.count; ++index
{
    var value = numbers[index];
    
        
    
        print(numbers)
      
    
    
    
}
let names = ["Chris", "Alex", "Ewa", "Barry", "Daniella"]


func sortArray(theString:String , theString2:String) -> Bool
{
    
    
    return theString > theString2
    
}


var strArray = ["P","Q","R","S","T","P","R","A","T","B","C","P","P","P","P","P","C","P","P","J"]

var filter = Dictionary<Int,Int>()

var arrayCount = numbers.count;

for var indexx = 0; indexx < arrayCount; ++indexx {
    
    var value = numbers[indexx];
    
    if (filter[value] != nil)
    {
        print(filter[value])
        numbers.removeAtIndex(indexx--);
        
        arrayCount--;
    }else
    {
        filter[value] = 1;
    }
    
}
print(numbers)

func findDuplicates(theArray: [Int]) -> [Int]
{
    var duplicates: [Int] = []
    
    var prevItem: Int = 0;
    
    for item in theArray{
        
        if(prevItem == item){
            duplicates.append(item);
          
        }
        prevItem = item;
       
        
        
       print(duplicates)
    }
    
   
    
    return duplicates;
    

    
}

var dupes = findDuplicates(numbers);



func compare(firstArray:NSArray,secondArray:NSArray) -> Bool
{
    for var index = 0; index <= firstArray.count; index++
        {
        
        
    }
   
    return true;
}

var i:Int;
for i in 1...30{
    
    if(i % 3 == 0 && i % 5 == 0){
        print(fizzBuzz);
    }
    if (i % 3 == 0){
        
        print(fizz);
    }
    if (i % 5 == 0){
        print(buzz);
        
    }
    else {
        print(i);
    }
}

print("area = \(calculateArea(12, width: 10))");
calculateArea(15000, width: 200000);

// accepts two int parameters
//Figures out if the first parameter is divisible by the second
//Returns a Bool optional 
//If the number is divisible it returns a true else it returns a nil
//call the function with 2 numbers and then print out whether "divisible or not divisible

func multiply (valueOne valueOne:Int, valueTwo:Int) ->Int{
    
    return valueOne * valueTwo;
    
    
}


multiply(valueOne: 20, valueTwo: 300);



func isDivisible (dividend dividend:Int, divisor:Int) -> Bool? {
    
    if( dividend % divisor == 0) {
        
        return true;
        
    }else {
        
        return nil;
    }
    
}

if let result = isDivisible(dividend: 100, divisor: 20)
{
    
    print("Is Divisible")
    
}else {
    
    print()

}

*/
